# natural-language-compiler
The AIORM or NLORM idea. Let people write prompts instead of queries, mutations, schemas, and migrations. Use LLMs to compile the prompts into fully functional backends that can target any infra the user chooses (sql/nosql, schema or schemaless, ORM or graphql or raw SQL, etc.)
